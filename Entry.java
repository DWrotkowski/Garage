package garage;


public class Entry {

	public static void main(String[] args) {
		//main method simply runs the System then exits once control is returned
		//This ensures there is only one way in and out of the system
		Garage.start();
		System.exit(0);
	}

}
